'use client';

// This is a placeholder file to replace any cached version
// since Next.js 13+ app directory doesn't use _document.tsx
// The actual font loading is in pages/_document.tsx

export default function DocumentPlaceholder() {
  return null;
}